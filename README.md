- Create a readme file and answer this question on your own. Don’t copy-paste from Google or any AI chatbot.
  - 1️⃣ What is the difference between var, let, and const?
  - Answer: The difference between var, let, and const is there ... var is old js function scoped. Let Introduced in ES6, it is block-scoped (lives only inside {}) ... let block scoped(use only for variables that change). const also block-scoped . variables never changed. only assign value to a const . not reassign..
  - 2️⃣ What is the spread operator (...)?
  - Answer: Spread operator is the array or object or function used only.. sumrize
    - arrays const kausar = [...sum1, ...sum2]; (joins tow arrays)
    - objects const kausar = {...user, age:25}; (objects copy and update property)
    - functions const Math.min(...numbers); (individulal arguments)
  - 3️⃣ What is the difference between map(), filter(), and forEach()?
    -The difference between map(), filter() and forEach() .. map creates a new array for the transforming every item. filter creates a new array but only items that pass a test .. forEach just loops items and not returning new array
  - 4️⃣ What is an arrow function?
    - Arrow function provide sorter syntas and hanlde the this keyword differently tradition function.
      syntax ::
      const sum = (a,b) => a + b;
  - 5️⃣ What are template literals? - Template Literals: Using backticks (`) instead of quotes.. ${variable} used for this project by template literals used there backticks used.
    - viz:
      const name = "Kausar";
      console.log(`Hello, my name is ${name}.
I'm junior web developer.`);
